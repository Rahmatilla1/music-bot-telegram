name: keep-render-awake
on:
  schedule:
    - cron: "*/10 * * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping render
        run: curl -sS https://music-bot-telegram-x4ea.onrender.com/health > /dev/null
