name: keep-render-awake
on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: ping render
        run: curl -sS https://music-bot-telegram-z2p0.onrender.com/health > /dev/null
